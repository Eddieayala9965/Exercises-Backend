import { useEffect, useState } from "react";
import { getTasks } from "../services/api";

const TaskList = () => {
  const [tasks, setTask] = useState([]);

  useEffect(() => {
    const fetchTasks
  }, []);

  return (
    <>
      <div></div>
    </>
  );
};
